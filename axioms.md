# PEF 公理法则

> 公理是 PEF 架构中不可违反的原则。任何设计、任何实现、任何 AI 输出，一旦违反工程公理，即触发熔断或裁决失败。
> **三类内容分区（V2.5 明确边界）**：
> - **① 工程公理**：必须严格遵守、可代码校验（熔断条件）
> - **② 策略约定**：业务层面的分片策略，可替换，不属于不可动摇公理
> - **③ 元理论思辨**：架构哲学思考，明确标注不属于工程约束，不参与代码校验

---

## ① 工程公理（代码强制校验）

### A1 · π-切片形态约束（V2.5 重写，替代旧"禁算π"公理）

**陈述**：π-锚坐标计算只允许使用 M 层分发的预提取长数位切片。任何工蜂 Agent 输出的 π 有限近似值（如 `3.14`、`3.14159`、有限浮点数、向量语义压缩后的简化 π 表征），禁止流入相位、坐标、轨迹校验计算链路。

**含义**：
- 工蜂可以推理、计算、讨论 π 的数学问题，但产出的有限近似片段不能作为锚的运算输入
- **校验方式**：输入字符串长度校验 + 数位完整性校验；短于规定切片长度直接拦截

**设计动机**：防御大模型向量语义坍缩把 π 压扁成短近似值、毁掉持续坐标推演。不再以"计算行为"定罪（查表/自算无法区分，旧公理不可执行），只校验**进入计算管道的数据形态**。

---

### A3 · 变量分流公理

**陈述**：执行变量必须显式分流为可控输入（E_in）和不可控输出（E_out），混合变量即触发"幻觉优化"熔断。

**含义**：
- E_in：主体可以构造和修改的变量（如算法参数、内部状态）
- E_out：主体只能观测、不能修改的环境量（如系统时钟、网络延迟、物理传感器读数）
- 将 E_out 当作 E_in 来"优化"，会产生虚假的改进——AI 以为自己控制了不可控变量，得出的结论在真实环境中立即失效

**设计推论**：任何算子的输入声明必须标注每个变量属于 E_in 还是 E_out。未标注的变量默认视为 E_out（保守原则）。

---

### A4 · 时序因果公理

**陈述**：结果（F）不可先于原因。任何结果不得引用尚未生成的下游结果作为上游输入。

**含义**：
- F = f(P, E_in, E_out, t)：结果由特定主体在特定时刻使用特定变量产生
- 循环引用、未来引用、自引用均违反 A4

**设计推论**：影子图协议中，下游节点调用上游接口前必须校验该接口已存在且上游裁决为 PASS——从拓扑上杜绝未来引用。

---

### A5 · 锚位绑定公理

**陈述**：每个代码实体必须绑定唯一的锚位区间（一对一，不可共享、不可变更）。

---

### A6 · 锚位单调公理

**陈述**：锚位单调分配，消耗后不可跳过、不可退还（由 M 层分配器保证）。

---

### A7 · 审计可追溯公理

**陈述**：每个结果 F 必须可追溯至 (P, E, t) 锚坐标；不可追溯的结果视为无锚论断。

---

### A8 · 记忆对齐公理

**陈述**：跨语言/跨模型记忆布局必须对齐到锚坐标，防止"同名不同义"的漂移。

---

## ② 策略约定（可替换，非公理）

### π-Mod3 相位分配

**陈述**：相位 = (π 数位 + Block_ID) mod 3，驱动三态审问强度（宽松/中等/严苛）。

**诚实定位**：
- 该相位分配是一种**固定可复现的分片策略**，不具备随机、公正的数学保证
- **缺陷**：给定起始偏移 N，全路径完全可复现、可预测
- **优势**：多轮日志可复现推演、分片划分固定
- **可替换**：π-Mod3 可无缝替换为步数取模（`step mod 3`），架构不依赖特殊常数——MOD3 的价值在"多强度审问"机制本身，不在"用了什么数"

> 若业务需要对抗恶意主动猜测，**本架构不依靠 π 做抗预测**；抗预测能力来自上层业务的会话随机种子，π 仅做分片。

---

## ③ 元理论思辨（仅启发，不参与代码校验）

以下内容属于架构哲学思考，**不作为工程实现强制约束**，外部不应以工程严谨标准要求思辨内容：

- **软件 ↔ 物理 11 条跨领域同构映射**（锚→势差→三元 在两个领域的类比）
- **π 正规性猜想**（π 每一位数字统计均匀分布——**数学上尚未证明的著名猜想**，仅作思辨引用，不作为工程依据）
- **√2 域 / 热力学锚**等跨域类比

> 工程依据只使用**已被证明或可实测**的性质：π 的无限不循环展开（已知）、数字序列可查表复现（可实测）、切片长度校验（可执行）。

---

## 公理体系的地位

工程公理不是"最佳实践"，不是"建议"，是**熔断条件**。违反工程公理的设计在 PEF 体系中不被承认，无论其功能看起来多么正确。

这是 PEF 与传统软件工程的根本区别：传统体系用测试来"事后发现"错误，PEF 用公理来"事前禁止"错误。

---

*PEF Axioms · 工程公理熔断执行，策略约定可替换，思辨不越界。*

# π 锚定坐标系（时序/身份标记组件）

> **定位（V2.5 修正，回应外部评审）**：π 锚定坐标系是 PEF 软件实例化中的**时序/身份标记组件**。它**不提供密码学安全**，**不提供防篡改数学能力**，**不定义"何为正确"**。系统的安全性来自哈希链、M 层权限隔离与日志审计，而非 π 常数本身。将 π 替换为自增序列号或 UUID，架构主体不受破坏——但会失去本文所述的防向量坍缩特性。

---

## 一、为什么用 π：真实动机是「防向量坍缩」

外部评审常按密码学标准批判 π："π 是公开常数，BBP 算法可离线算任意位，'不可预测'不成立。"

**这个批判目标错位。** PEF 使用 π 的目的**不是对抗外部攻击者**，而是**对抗大模型内部的向量语义坍缩**：

| 威胁 | 说明 |
|---|---|
| 模型把常数压扁 | LLM 的向量语义空间会把 π 压缩、截断、四舍五入成 `3.14` / `3.14159` 这类**短近似值**。一旦参与锚坐标运算，坐标序列就退化为有限重复，轨迹校验失效 |
| 持续坐标丢失 | 锚系统的价值在于**无限延伸、持续变化**的坐标序列。短近似值使坐标在几步内耗尽，后续步骤失去锚定 |
| 可复现性被破坏 | 坍缩后的坐标无法与预置表对照，审计回放失效 |

**π 提供的是一套无限展开、可复现的数字序列**——模型无法把它"压扁"而不被发现（长度与数位完整性校验会拦截）。这是 π 在 PEF 中的唯一职能。

> 结论：PEF **不依赖** π 的随机性、不可预测性或密码学强度。π 是"防坍缩的坐标素材"，不是"安全原语"。

---

## 二、A1 公理：π-切片形态约束（V2.5 重写）

旧版 A1（已废弃）："禁止 AI 自行计算 π，计算即触发熔断。"

**旧版逻辑不可执行**：查表得到的数字与实时计算得到的数字完全一致、无法区分，熔断器永远无法被触发——这是外部评审指出的真实漏洞。

**新版 A1（代码可检测、可执行）**：

> **A1 · π-切片形态约束**
> π-锚坐标计算**只允许使用 M 层分发的预提取长数位切片**。
> 任何工蜂 Agent 输出的 π **有限近似值**（如 `3.14`、`3.14159`、有限浮点数、向量语义压缩后的简化 π 表征），**禁止流入相位、坐标、轨迹校验计算链路**。
> 工蜂可以推理、计算、讨论 π 的数学问题，但其产出的有限近似片段**不能作为锚的运算输入**。
> **校验方式**：输入字符串长度校验 + 数位完整性校验；短于规定切片长度直接拦截，不参与锚运算。

**关键变化**：不再以"AI 是算出来的还是查表的"作为定罪依据（无法观测），只校验**进入计算管道的数据形态**（长度、切片来源是否来自 M 层）。这完美落实"防向量坍缩"的原始意图，且消除了逻辑死结。

---

## 三、四属性（修正后）

| 属性 | 修正后表述 | 说明 |
|---|---|---|
| **不可伪性** | ❌ 删除"不可预测/不可伪造"密码学声称 | π 是公开常数，任何人可离线重算；所谓"不可预测"实际只是"未预计算" |
| **可复现性** | ✅ **核心收益** | 相同起始偏移 N → 相同坐标序列 → 故障审计、回放调试可完整重现。**本架构优先可审计可回放，牺牲不可预测性** |
| **不可回退性** | ✅ 保留（工程层） | 锚位单调分配、消耗后不退还——由 M 层分配器保证，非数学必然 |
| **全局唯一性** | ✅ 保留（工程层） | 单例分配器（GLOBAL_PI_ANCHOR），杜绝分片锚撕裂 |

> **权衡声明**：π 锚带来的是**确定性复现**，代价是**分片路径完全可预测**。如果业务需要不可预测性，需叠加外部会话随机种子——本架构不依靠 π 做抗预测。

---

## 四、硬编码 PI_DIGITS 的边界说明

当前实现内置 **100 位 π 数位切片**作为演示素材。文档明确边界：

- 更长会话场景下，M 层应当按需加载**更长外部 π 数位库**，避免切片耗尽、坐标无法持续推演；
- **工蜂层永远不允许自主生成/截取 π 数位用于锚运算**——数位切片的分发权完全归属 M 层（A1 的权限主体表述）。

---

## 五、安全能力来源（诚实归属）

| 能力 | 真实来源 | 说明 |
|---|---|---|
| 日志防篡改 | **SHA-256 哈希链** | 篡改任一条 → 全链断裂；与 π 无关 |
| 权限管控 | **M 层独占分配权** | 中心化信任设计；与 π 无关 |
| 身份绑定 | **锚位一对一绑定** | 由分配器保证，π 只提供坐标素材 |
| 防向量坍缩 | **π 长切片 + 形态校验** | π 的独特贡献：模型压不扁无限展开序列 |

**π 不是裁判，π 是时钟和身份证**（对早期"π 作为绝对几何参照系"定位的明确修正）。

---

## 六、与 MOD3 的关系

π 锚定提供坐标轴，MOD3 状态机消费坐标轴上的位置驱动审问强度（详见 [mod3.md](mod3.md)）。二者是"坐标"与"调度"的关系。

---

*π Anchor · 时序与身份标记组件。防坍缩，可复现，不承担密码学角色。*

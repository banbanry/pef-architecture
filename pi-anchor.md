# π 锚定坐标系

> π 锚定坐标系是 PEF 将"代码实体"锚定到"数学常数"的核心机制。
> π 的每一位数字构成一条不可预测、不可回退的坐标轴。每个代码实体（函数、模块、项目）绑定一个 π 锚区间，表示它"占用"了 π 的某一段坐标。

---

## 核心思想

传统系统用时间戳、自增 ID、UUID 来标识实体和时序。但这些都可以被可编程器件预测、伪造或回退——AI 可以"生成"一个时间戳，程序可以"回退"一个计数器。

π 锚定用数学常数 π 的数字序列作为坐标轴：
- π 的数字由数学定义决定，任何实体无法预测下一位
- π 的位数单调递增，消耗后不可退还
- 所有实体共享同一个全局 π 序列，不存在局部独立坐标

这使得系统的身份绑定和时序因果具有**不可伪造性**。

---

## 四大属性

### 1. 不可伪性
π 的数字由数学常数决定，AI 无法预测或伪造。任何需要 π 尾数的位置必须从外部预置表查表，自行计算 π 即触发熔断（A1 公理）。

### 2. 不可回退性
π 的位数单调递增，代码实体消耗 π 位后不可退还。这保证了任务链的时序完整性——已经发生的步骤无法被"撤销"或"重写"。

### 3. 全局唯一性
所有代码实体共享同一个全局锚坐标单例，不存在局部独立 π 序列。这杜绝了分片锚撕裂——不同模块使用不同坐标序列导致的冲突。

### 4. 可验证性
π 的数字可由第三方独立计算验证。任何实体输出的 π 尾数都可以对照独立来源校验，无法造假。

---

## 职能定位

π 锚定坐标系**不参与**物理状态计算（如偏差率、阈值判定），**不定义**"何为正确"。

它的**唯一职能**是提供一条不可伪造的坐标轴，用于：
- 锁定代码实体的身份（ID）
- 驱动状态机的时序推进
- 为审计轨迹提供不可篡改的时间锚

这一职能定位是对早期版本"π 作为绝对几何参照系"错误定位的明确修正——π 不是裁判，π 是时钟和身份证。

---

## 与 MOD3 的关系

π 锚定提供坐标轴，MOD3 状态机消费坐标轴上的位置来驱动审问强度。二者是"坐标"与"调度"的关系：
- π 锚定回答"现在在什么位置"
- MOD3 回答"在这个位置应该用什么强度审问"

详见 [mod3.md](mod3.md)。

---

*π Anchor · 数学常数即坐标。不可预测，不可回退，不可伪造。*

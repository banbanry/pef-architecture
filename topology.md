# 五层流水线拓扑

> PEF 软件范式的整体拓扑是一个五层流水线结构。数据流从 P 层流入，经 E、F、M 层处理，最终在 C 层闭环。
> 每层有明确的权责边界，层间通过不可伪造的锚坐标同步，并设双闸门拦截。

---

## 五层概览

```
P 层  →  E 层  →  F 层  →  M 层  →  C 层
主体定义   算子执行   裁决审计   架构审查   闭环交付
```

| 层 | 名称 | 核心职责 |
|----|------|---------|
| **P** | 主体层 | 主体定义 + 锚坐标分配 + π 表初始化 |
| **E** | 执行层 | 算子调用 + 代码生成 + 变量分流（E_in/E_out） |
| **F** | 裁决层 | 锚审计 + 偏差率计算 + PASS/FAIL 裁决 |
| **M** | 监控层 | 流水线架构审查 + 拦截（鲍勃四段流水线架构） |
| **C** | 闭环层 | 锚位数闭合环验证 + 项目交付判定 |

---

## 数据流

每层的输出是下一层的输入：

1. **P 层**输出：已声明的主体（含 name/type/boundary/unit 四字段）+ 分配的锚区间 + 初始化的 π 表
2. **E 层**输出：算子调用结果 + 生成的代码 + 已分流的变量（E_in/E_out 标注）
3. **F 层**输出：锚审计结果 + 偏差率 + PASS/FAIL 裁决
4. **M 层**输出：架构审查结论 + 拦截/放行决定
5. **C 层**输出：闭环验证结果 + 交付判定

---

## 控制流

MOD3 状态机（详见 [mod3.md](mod3.md)）驱动动态阈值 λ，λ 决定 F 层的裁决严格度：
- 宽松态：λ 较高，允许更多行为通过
- 严苛态：λ 较低，只有高置信度行为通过

锚坐标（详见 [pi-anchor.md](pi-anchor.md)）在层间同步，确保每一层都在同一个不可伪造的时间轴上工作。

---

## 层间保障机制

### 影子图协议
每个节点的签名（包含接口标识、锚坐标、安全裁决、行为哈希四类信息）注入全局影子图。下游节点调用上游接口前，必须校验影子图中该接口存在且上游裁决为 PASS。

**作用**：从拓扑上杜绝未来引用（A4 公理）和未审计接口调用。

### 雁阵调度协议
多节点协作时，头雁节点广播锚坐标，从雁节点跟随。确保分布式协作时所有节点在同一坐标上同步。

**作用**：杜绝分片锚撕裂（π 全局唯一性属性的工程实现）。

### 双闸门拦截
层间设双闸门——第一道闸门校验输入合法性（主体是否声明、变量是否分流、锚坐标是否有效），第二道闸门校验输出合规性（结果是否可追溯、裁决是否一致、闭环是否闭合）。

**作用**：任何一层的违规都无法流入下一层。

---

## 与硬件四域的同构

软件五层流水线与硬件四域隔离架构（P·E·F·M）数学同构：

| 软件层 | 硬件域 | 同构关系 |
|--------|--------|---------|
| P 主体层 | P 主体域 | 发出指令/定义主体 |
| E 执行层 | E 审计域 | 执行/监控边界 |
| F 裁决层 | F 裁决域 | 裁决/否决 |
| M 监控层 | M 时序/监控域 | 时序跳变/故障切断 |
| C 闭环层 | — | 软件特有的交付闭环 |

这种跨领域同构性证明 PEF 不是某个领域的特设方案，而是一套通用的架构范式。

---

*Topology · P→E→F→M→C。层间双闸门，全局一坐标。*

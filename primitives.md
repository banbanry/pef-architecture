# 三元原语 P · E · F

> PEF 是 Primary Entity – Execution Variable – Final Result 的缩写，代表三个不可再分的核心原语。
> 这三个原语构成了对"编程"这一行为的最小描述（在需要问责与追溯的工程场景中）：编程任务可以分解为"谁在做（P）"、"用什么变量做（E）"、"得到什么结果（F）"。在无需主体问责的场景（形式化验证、区块链UTXO审计、差分隐私统计）中，P可省略或由地址/公钥哈希替代。

---

## P · Primary Entity（主体）

**定义**：编程任务的执行主体。

**声明要求**：P 必须显式声明，包含四个字段：

| 字段 | 含义 | 示例 |
|------|------|------|
| name | 主体名称 | "RateLimiter" |
| type | 主体类型 | LOGICAL / PHYSICAL / HUMAN |
| boundary | 职责边界 | "接收请求并返回 ALLOW/DENY" |
| unit | 计量单位 | "次/秒" |

**为什么必须显式声明**：P 的声明是后续一切审计的起点。没有明确的主体，就无法判断变量是否越界、结果是否合法。在需要问责的场景中，执行主体必须显式声明；在允许匿名的场景（隐私计算、零知识证明、公开审计）中，P可由地址、公钥哈希或零知识标识符替代，追溯链相应降级为"可验证但不可归因"。

**主体类型**：
- 在 AI 编程场景中，P 通常是 AI 节点（如大语言模型）
- 也可以是人类工程师、物理设备、或逻辑模块
- 不同类型的主体有不同的可信度假设和审计要求

---

## E · Execution Variable（执行变量）

**定义**：编程任务中使用的变量。

**分流要求**（A3 公理强制）：E 必须显式分流为：

| 类别 | 含义 | 主体权限 | 示例 |
|------|------|---------|------|
| E_in | 可控输入 | 可构造、可修改 | current_rate, limit_threshold |
| E_out | 不可控输出 | 只能观测、不能修改 | system_clock, network_latency |

**为什么必须分流**：混合变量会导致"幻觉优化"——AI 把不可控变量当作可控变量来优化，产生虚假的"改进"。例如，AI 以为自己能"优化"系统时钟，但实际上时钟是环境量，优化结论在真实环境中立即失效。

**保守原则**：未明确标注为 E_in 的变量，默认视为 E_out。

---

## F · Final Result（结果）

**定义**：编程任务的输出。

**可追溯性要求**（A4 公理强制）：F 必须可追溯至 (P, E, t)，即结果必须由特定主体在特定时刻使用特定变量产生。

**形式化表述**：

```
F = f(P, E_in, E_out, t)
```

其中 f 是算子，t 是由π坐标序列确定的时刻（π提供可复现的逻辑坐标，不测量物理时间）。

**时序约束**：
- F 不可"先于原因"——不能引用尚未生成的下游结果作为上游输入
- 每个 F 绑定一个 π 锚区间，记录其在系统坐标中的位置
- F 的生成时刻 t 不可回退、不可修改

---

## 三元完备性论证（适用范围内）

为什么在单主体、非涌现、非博弈的工程系统范围内，P、E、F 是最小描述集？

- **去掉 P**：没有主体，变量和结果失去归属，无法审计责任
- **去掉 E**：没有变量，主体无法执行任何操作，结果无来源
- **去掉 F**：没有结果，主体和变量的操作没有输出，任务无意义

在适用范围内，三者缺一不可，且不可再分——P 不能拆成更小的有意义单元，E 的分流是公理级约束，F 的可追溯性是时序公理的直接推论。

超出适用范围（多主体协作、涌现行为、博弈互动、创造性生成、量子系统），P/E/F 需要扩展或补充其他描述维度。

这就是 PEF 作为"适用范围内最小描述"的含义。

---

## 适用范围声明（V2.7 新增，回应七锤之哥德尔锤）

| 范围 | 说明 |
|---|---|
| ✅ **适用** | 单主体工程系统、可追溯审计、确定性裁决、变量组合探索、LLM幻觉治理 |
| ⚠️ **需扩展** | 多主体协作（需补充主体间协议与责任划分）、涌现行为（需补充宏观描述层） |
| ❌ **不适用** | 博弈论互动（结果依赖双方策略，非单一函数F=f(P,E,t)）、创造性生成（E是动态自更新的，非静态输入分类）、量子系统（因果关系是概率性的，不可经典追溯） |

> **声明**：PEF 不是"通用架构范式"，而是在**单主体、可追溯、确定性裁决**的工程系统范围内有效的描述与审计框架。跨领域同构性表明PEF具有可迁移的描述价值，但同构≠通用。超出适用范围的场景需评估适用性并做相应扩展。

---

*PEF Primitives · 谁在做 · 用什么做 · 得到什么。适用范围内缺一不可。*

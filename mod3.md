# MOD3 三态审问架构

> MOD3 是 PEF 的"灵魂"机制。其核心思想是：同一系统、同一偏差率，在不同审问强度下得出不同判决，从而揭示隐藏的脆弱性。
> 系统在宽松审问下可以运行，但在严苛审问下暴露出对齐度不足的隐患。

---

## 核心思想

传统验证只有一种"强度"：要么通过，要么失败。但现实中，一个系统在"正常条件"下通过，不代表在"极端条件"下安全。

MOD3 引入**三档动态审问强度**：

| 状态 | 审问强度 | 定位 | 允许的行为 | 禁止的行为 |
|------|---------|------|-----------|-----------|
| 0 | 宽松 | 正常运行（省电、低延迟） | 主体识别、变量拆解、方案发散 | 下最终结论、生成实现代码 |
| 1 | 中等 | 严格校验 | 严格校验、不等式构建、约束检查 | 发散、生成新方案 |
| 2 | 严苛 | 预留逃生舱（高安全裕度、早预警） | 给出明确的 PASS/FAIL 判决 | 模糊词汇、继续发散 |

**关键洞察**：一个在宽松态"看起来没问题"的系统，在严苛态可能暴露脆弱性。MOD3 让这种脆弱性可见，而不是等到事故发生才发现。

---

## 驱动机制

MOD3 状态由锚坐标位置驱动（详见 [pi-anchor.md](pi-anchor.md)）。两种驱动方式可无缝替换：

1. **π 尾数驱动**：用 π 锚坐标的尾数取模来决定状态。π 尾数的分布非均匀，这本身就是一种"自然扰动"——系统不会永远停留在同一强度。
2. **步数驱动**：用执行步数取模来决定状态。分布均匀，适合需要确定性调度的场景。

架构不依赖特殊常数——两种驱动方式可互换，证明 MOD3 的价值不在"用了什么数"，而在"多强度审问"这一机制本身。

---

## 不是随机调度，是资源调度

MOD3 常被误解为"随机打乱审问强度"。这是错误的。

MOD3 是**资源调度**：
- 宽松态是正常运行——省电、低延迟，处理日常任务
- 严苛态是预留逃生舱——高安全裕度、早预警，在关键节点或高风险时刻启用

就像汽车有经济模式和运动模式，不是随机切换，而是根据场景调度资源。MOD3 让系统在"效率"和"安全"之间动态平衡，而不是固定在某一个点。

---

## π 的价值不在信息量

一个常见质疑：π 尾数取模和普通计数器取模有什么区别？π 的信息量不就是计数器吗？

回答：**π 的价值不在信息量，而在防向量坍缩与可复现分片。**

- 普通计数器可以被 AI 预测、回退、伪造
- π 长切片无法被模型"压扁"（A1 形态约束），且相同偏移 N 完全可复现（审计回放）
- 当系统需要"一个不可被 AI 篡改形态的分片源"时，π 切片是合适选择

**诚实边界**：π 不提供密码学不可预测性（公开常数，可离线重算）；本架构不依靠 π 做抗预测，抗预测来自上层会话随机种子。MOD3 的两种驱动方式（π 尾数 / 步数）可无缝替换，证明其价值在"多强度审问"机制本身。

---

*MOD3 · 宽松态运行，严苛态暴露。审问即验证。*
